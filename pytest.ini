[pytest]
addopts = -m "not notebook and not local_integration"
markers =
    notebook: optional deeper checks that execute selected notebook cells
    local_integration: optional checks that require local config, data, models, and Ollama
