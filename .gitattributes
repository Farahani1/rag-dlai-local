# Normalise line endings: the repository stores LF, each checkout uses the
# platform default (CRLF on Windows with core.autocrlf=true).
* text=auto

# Binary artifacts
*.joblib binary
*.sqlite3 binary
*.bin binary
*.png binary
